import os

import os

path = "."
for entry in os.listdir(path):
    print(entry)
# This code lists all files and directories in the current directory
# You can change the `path` variable to list files in a different directory